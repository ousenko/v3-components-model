package agency.v3.components.model.entity;

/**
 * Represents an entity to use when instance is ignored, e.g. when you ignore {@link io.reactivex.Observable} emission and only care for the stream to start, process and complete
 * */
public enum Irrelevant {
    INSTANCE
}
